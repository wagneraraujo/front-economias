import styled from 'styled-components'

export const Container = styled.div`
  padding: 6px;

  > select {
    padding: 6px;
    background-color: #fff;
    border-radius: 4px;
    margin-left: 4px;
  }
`
